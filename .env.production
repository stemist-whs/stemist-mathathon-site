# Mount path where the app is served (no trailing slash). Matches client-side routes (react-router basename).
# Asset URLs use relative paths from Vite; this is only for routing under a subpath.
VITE_PUBLIC_BASE_PATH=/app/docs
