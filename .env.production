# Optional: basename for react-router when the app is served under a subpath (e.g. /app/docs).
# Leave unset for root URLs like http://127.0.0.1:9999/ or https://user.github.io/repo/
# For subpath deploys use: npm run build:subpath
# Or one-off: VITE_PUBLIC_BASE_PATH=/your/subpath npm run build
